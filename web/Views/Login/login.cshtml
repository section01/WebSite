<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="utf-8">
    <title>サイトタイトル</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="~/lib/jquery/dist/jquery.js"></script>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />
    <style>
        body {
            margin: 10px;
        }
        td.box {
            border: solid 1px;
            width: 100px;
            height: 100px;
        }
        td.space {
            border: 0 !important;
            width: 50px;
            height: 50px;
        }
    </style>
</head>
<body>
    <header>
    </header>
    <main>
        <center>
            <table class="seat">
                @for (var i = 0; i < 6; i++)
                {
                <tr>
                    <td class="box" id="@(i + 1)"></td>
                    <td class="box" id="@(i + 2)"></td>
                    <td class="space"></td>
                    <td class="box" id="@(i + 3)"></td>
                    <td class="box" id="@(i + 4)"></td>
                    <td class="space"></td>
                    <td class="box" id="@(i + 5)"></td>
                    <td class="box" id="@(i + 6)"></td>
                    <td class="space"></td>
                    <td class="box" id="@(i + 7)"></td>
                    <td class="box" id="@(i + 8)"></td>
                    <td class="space"></td>
                    <td class="box" id="@(i + 9)"></td>
                    <td class="box" id="@(i + 10)"></td>
                    <td class="space"></td>
                </tr>
                }
            </table>
        </center>
    </main>
    <footer>
    </footer>
</body>
</html>
